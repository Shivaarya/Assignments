function recursive(str){
    let count=0;
    for(let i=0;i<=str.length-1;i++){
    count++
    }
    console.log(count);
    }
    
    recursive("banana")
    
    function recursive(str){
    let count=0;
    for(let i=0;i<=str.length-1;i++){
    count++
    }
    console.log(count);
    }
    
    recursive("a")
    
    function recursive(str){
    let count=0;
    for(let i=0;i<=str.length-1;i++){
    count++
    }
    console.log(count);
    }
    
    recursive("apple")